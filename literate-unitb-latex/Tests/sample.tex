\begin{calculation}
	f.x \le f.y
\hint{=}{ $\le$ to $=$ and $\uparrow$ }
	f.x \uparrow f.y \,=\, f.y
\hint{=}{ $f$ over $\uparrow$ }
	f.(x \uparrow y) \,=\, f.y
\hint{\follows}{ Leibniz }
	x \uparrow y \,=\, y
\hint{=}{ $\le$ from $=$ and $\uparrow$ }
	x \le y
\end{calculation}